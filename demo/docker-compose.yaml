version: '3.8'

services:
  web:
    build: .
    command: uvicorn api:app --host 0.0.0.0 --port 8000

    # # for local library
    # volumes:
    #   - ./../src/pyPhraseSuggester:/app/pyPhraseSuggester:ro
    ports:
      - 80:8000